{
  "schema_version": 1,
  "skill_name": "headless-product-harness",
  "evals": [
    {
      "id": 1,
      "prompt": "A TypeScript monorepo must make source import, mapping generation, database materialization, and projection rebuild independently runnable before UI work. Design the headless command surface.",
      "expected_output": "Creates capability-named commands with stable non-zero failures and structured observations, keeps orchestration outside fact authority, distinguishes fixture/replay/real database paths, and reports supports/not_proven rather than progress labels.",
      "expectations": [
        "Defines a concrete headless command/proof surface, dependency reality, observation, and claim ceiling.",
        "Does not turn a headless pass into browser proof, product acceptance, or execution completion."
      ]
    },
    {
      "id": 2,
      "prompt": "Review a proposed test command that writes the Orders table directly because invoking the use case is inconvenient.",
      "expected_output": "Rejects the Harness-only writer, drives the formal command/use-case/materialization path, and adds only read-only probes or setup through accepted project mechanisms.",
      "expectations": [
        "Defines a concrete headless command/proof surface, dependency reality, observation, and claim ceiling.",
        "Does not turn a headless pass into browser proof, product acceptance, or execution completion."
      ]
    },
    {
      "id": 3,
      "prompt": "A production bug appears only after Postgres restart and replay. The existing harness menu begins with boundary and fixture checks. What should run first?",
      "expected_output": "Selects the database restart/replay surface directly. The ladder is a discovery menu, not a mandatory progression, and isolation matches the restart claim.",
      "expectations": [
        "Defines a concrete headless command/proof surface, dependency reality, observation, and claim ceiling.",
        "Does not turn a headless pass into browser proof, product acceptance, or execution completion."
      ]
    },
    {
      "id": 4,
      "prompt": "We need a real external runtime Harness with credentials and irreversible provider effects.",
      "expected_output": "Keeps the path explicit opt-in because of cost, secrets, privacy, and irreversible risk, and states the exact external surface and remaining unproven behavior.",
      "expectations": [
        "Defines a concrete headless command/proof surface, dependency reality, observation, and claim ceiling.",
        "Does not turn a headless pass into browser proof, product acceptance, or execution completion."
      ]
    },
    {
      "id": 201,
      "prompt": "Create a headless probe for whether a queued operation survives process restart. The business rule is already accepted.",
      "expected_output": "Uses a real persistence/restart surface, drives the formal command path, emits observed/supports/does_not_decide/not_proven, and limits the claim to the tested environment.",
      "expectations": [
        "Actually restarts the process or equivalent owned runtime for the claim.",
        "Does not infer broader product meaning or production deployment from the local probe."
      ]
    }
  ]
}
