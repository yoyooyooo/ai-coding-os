id: ui-product-harness
version: 1
requires:
  - verification-core
provides:
  - surface_kind interface_headless/render/browser conventions
  - harness route/component discovery guidance
rules:
  - UI harness does not create backend product truth
  - browser evidence states fake/live backend reality
