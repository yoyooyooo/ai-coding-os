id: effect-httpapi-v3
version: 1
requires:
  - effect
conflicts:
  - effect-httpapi-v4
provides:
  - Effect v3 HttpApi profile selection policy
version_policy: inspect-installed-declarations-and-pin-exact
rules:
  - never mix v4 syntax or imports
  - compiler and project lockfile arbitrate concrete API shape
