# Engineering Standards

## Current Standards

| Standard | Scope | Enforcement or review surface |
| --- | --- | --- |
| `source-topology-and-naming.md` | source directories, semantic filenames, public/private imports | `<lint, architecture check, review, or none>` |
| `architecture-profile.yaml` | compact selected architecture defaults and exceptions | `<consumer or human-only>` |
| `naming-vocabulary.yaml` | canonical project terms and implementation qualifiers | `<consumer or review>` |
| `verification-policy.md` | stable verification entry points and claim limits | `<commands or CI>` |

Delete absent rows. Do not create files only to complete the table.

## Scope

These Standards bind work only within their stated scope. Product meaning remains with product authority; observed behavior remains bounded by executed evidence.

## Project Overrides

- `<portable default>` is replaced by `<local convention>` because `<pressure>`; protected invariant: `<invariant>`.

## Proposed or Experimental Practices

Keep candidates outside this Current Home until accepted. Link them here only when readers repeatedly need the route.
