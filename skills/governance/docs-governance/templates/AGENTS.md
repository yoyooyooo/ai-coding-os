# Repository Agent Guide

> Keep this file thin. Delete sections that do not carry stable project meaning.

## Project authority routes

- Product meaning: `<path or external system>`
- Shared language and invariants: `<path>`
- Binding engineering standards: `<path>`
- Architecture profile: `docs/standards/architecture-profile.yaml` or `<project equivalent>`
- Current architecture: `<path>`
- Documentation router: `docs/README.md`

## Resolved commands

- Install: `<command>`
- Format/lint: `<command>`
- Static/type check: `<command>`
- Unit/integration tests: `<command>`
- Architecture/boundary check: `<command or not present>`
- Affected verification: `<command>`
- Full verification: `<command>`

## Stable local constraints

- `<constraint that materially changes implementation or verification>`

## Restricted, generated, or sensitive paths

- `<path>` — `<rule>`

## Language policy

- Durable narrative prose: `<language policy>`
- Paths, commands, protocols, schemas, and code symbols: English unless an external contract requires otherwise.

## Intentional deviations from portable defaults

- `<local convention>` replaces `<portable default>` because `<pressure>`; the protected invariant is `<invariant>`.
