# Documentation Router

## Current Homes

| Question | Current Home |
| --- | --- |
| Product outcomes, scope, rules, quality, and acceptance | `product/` |
| Shared language, objects, states, and invariants | `ssot/` |
| Binding engineering and policy rules | `standards/` |
| Current architecture, runtime, and composition | `architecture/` |

Delete rows for absent homes. Add conditional homes only when they exist.

## Project-specific topology

- `<local-name>/` maps to the portable role `<role>`.
- `<deliberate deviation and why it exists>`.

## Entry routes

- From source: `<module or repository route>`
- From failures: `<verification or runbook route>`
- From product terms: `<glossary or SSoT route>`

## Freshness and conflicts

- `<how readers identify stale or superseded material>`
