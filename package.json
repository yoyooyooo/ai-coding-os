{
  "name": "ai-coding-os-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AI Coding OS suite with Goal Proof skills and CLI.",
  "type": "module",
  "packageManager": "bun@1.2.17",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter goal-proof build",
    "typecheck": "bun run --filter goal-proof typecheck && tsc --noEmit -p tsconfig.json",
    "test": "bun run --filter goal-proof test && bun test scripts",
    "check:skills": "python3 skills/tooling/suite_audit.py --suite skills && python3 skills/governance/docs-governance/scripts/run_docs_audit.py --repo .",
    "check": "bun run build && bun run typecheck && bun run test && bun run check:skills",
    "pack:dry": "bun run --filter goal-proof pack:dry",
    "install:local": "bun run --filter goal-proof link:local",
    "release:check": "bun scripts/release.ts --precheck",
    "release": "bun scripts/release.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.17",
    "typescript": "^5.8.3"
  }
}
