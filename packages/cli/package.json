{
  "name": "goal-proof",
  "version": "0.2.0",
  "description": "Experimental Goal Proof CLI for evidence-backed durable coding goals.",
  "type": "module",
  "license": "MIT",
  "homepage": "https://github.com/yoyooyooo/ai-coding-os#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yoyooyooo/ai-coding-os.git",
    "directory": "packages/cli"
  },
  "bugs": {
    "url": "https://github.com/yoyooyooo/ai-coding-os/issues"
  },
  "keywords": [
    "ai",
    "agent",
    "cli",
    "goal-pack",
    "goal-proof",
    "coding-agent"
  ],
  "bin": {
    "goal-proof": "dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist/",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "mkdir -p dist && bun build src/goal-proof.ts --target=node --outfile=dist/cli.js && bun build src/index.ts --target=node --outfile=dist/index.js && chmod +x dist/cli.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "check": "bun run build && bun run typecheck && bun run test",
    "link:local": "bun link",
    "pack:dry": "npm pack --dry-run",
    "prepack": "bun run check"
  },
  "dependencies": {
    "commander": "^14.0.3"
  }
}
